import TaskListContainer  from "../../features/tasks/containers/TaskListContainer";

export default function TaskListPage() {
    return (
        <div>
            <TaskListContainer />
            {/* タスク一覧を表示するコンテナコンポーネントを呼び出す */}
        </div>
    );
}